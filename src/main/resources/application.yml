
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test9?serverTimezone=Asia/Seoul
    username: root
    password: 1234
  main:
    allow-circular-references: true
  jpa:
    hibernate:
      ddl-auto: update #create update none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        globally_quoted_identifiers: true
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  config:
    import: application-oauth.yml

jwt:
  secretKey: asdasdasdasdasdadadadadadadadadadadadafafafafasfasfasfasfafafafs
  access:
    expiration: 2000000 # 1시간(60분) (1000L(ms -> s) * 60L(s -> m) * 60L(m -> h)) 1000 * 200
    header: Authorization

  refresh:
    expiration: 1209600000 #  (1000L(ms -> s) * 60L(s -> m) * 60L(m -> h) * 24L(h -> 하루) * 14(2주))
    header: Authorizationrefresh